# Takara Gold - Deployment Configuration Example
# Copy this file to deploy.env and fill in your actual credentials
# NEVER commit deploy.env to git!

# Server Configuration
SERVER_IP="your.server.ip.address"
SERVER_USER="your-ssh-username"

# OPTION 1: SSH Password (NOT RECOMMENDED - use SSH keys instead)
# SERVER_PASS="your-ssh-password"

# OPTION 2: SSH Key (RECOMMENDED)
# Setup SSH key authentication:
# 1. Generate SSH key: ssh-keygen -t ed25519 -C "your_email@example.com"
# 2. Copy to server: ssh-copy-id -i ~/.ssh/id_ed25519.pub user@server
# 3. Leave SERVER_PASS empty or comment it out
# 4. Update deploy.sh to remove sshpass and use ssh directly

# If using SSH keys, modify deploy.sh to replace:
#   sshpass -p "${SERVER_PASS}" ssh ...
# with:
#   ssh -i ~/.ssh/id_ed25519 ...
