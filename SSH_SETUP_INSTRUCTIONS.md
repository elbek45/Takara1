# 🔐 SSH Setup Instructions

## Проблема
Парольная аутентификация на сервере отключена. Нужно добавить SSH ключ для безопасного подключения.

## ✅ Решение: Добавить SSH ключ на сервер

### Вариант 1: Через консоль DigitalOcean (Рекомендуется)

1. Зайдите на [DigitalOcean](https://cloud.digitalocean.com/)
2. Найдите ваш дроплет `159.203.104.235`
3. Нажмите **"Access"** → **"Launch Droplet Console"**
4. В консоли выполните:

```bash
# Создайте директорию .ssh если её нет
mkdir -p ~/.ssh
chmod 700 ~/.ssh

# Добавьте публичный ключ
echo "ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIPyb48W6Uuus1A9Pfz8L5K+7rFrWuk36ZJs/mbT7cUR2 takara-deployment" >> ~/.ssh/authorized_keys

# Установите правильные права
chmod 600 ~/.ssh/authorized_keys

# Проверьте
cat ~/.ssh/authorized_keys
```

5. Закройте консоль

### Вариант 2: Через DigitalOcean SSH Keys

1. Зайдите в **Settings** → **Security** → **SSH Keys**
2. Нажмите **"Add SSH Key"**
3. Вставьте публичный ключ:
```
ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIPyb48W6Uuus1A9Pfz8L5K+7rFrWuk36ZJs/mbT7cUR2 takara-deployment
```
4. Название: `Takara Deployment Key`
5. Нажмите **"Add SSH Key"**
6. В настройках дроплета добавьте этот ключ к серверу

---

## 🧪 Тест подключения

После добавления ключа проверьте подключение:

```bash
ssh root@159.203.104.235 'echo "SSH connection successful!"'
```

Если увидите "SSH connection successful!" - всё готово для деплоя!

---

## 🚀 Следующие шаги после настройки SSH

1. **Загрузить setup-server.sh на сервер:**
```bash
cd /home/elbek/TakaraClaude/takara-gold
scp setup-server.sh root@159.203.104.235:/root/
```

2. **Запустить setup скрипт на сервере:**
```bash
ssh root@159.203.104.235
chmod +x /root/setup-server.sh
./setup-server.sh
```

3. **После успешной настройки сервера - задеплоить код:**
```bash
cd /home/elbek/TakaraClaude/takara-gold
./deploy.sh
```

---

## 🔄 Альтернатива: Включить парольную аутентификацию

Если предпочитаете использовать пароль (не рекомендуется):

1. Подключитесь через DigitalOcean Console
2. Отредактируйте SSH config:
```bash
nano /etc/ssh/sshd_config
```

3. Найдите и измените:
```
PasswordAuthentication yes
PermitRootLogin yes
```

4. Перезапустите SSH:
```bash
systemctl restart sshd
```

**⚠️ ВАЖНО:** Парольная аутентификация менее безопасна. Лучше использовать SSH ключи.

---

## 📞 Помощь

Если возникли проблемы:
- Проверьте логи SSH: `tail -f /var/log/auth.log` (на сервере)
- Проверьте права на файлы: `ls -la ~/.ssh/` (на сервере)
- Убедитесь что firewall разрешает SSH: `ufw status` (на сервере)
